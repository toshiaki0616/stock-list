# AI開発ワークフロー

## 目的

会話で決めた内容を、実装・検証・履歴・文書まで一貫して残す。  
標準の流れは **ChatGPT → GitHub Issue → Codex → PR / Review → ドキュメント更新** とする。

## 役割

| 担当 | 主な責任 |
| --- | --- |
| 業務担当者 | 目的、業務上の判断、受入れ確認を伝える |
| ChatGPT | 要件整理、設計、影響範囲、受入条件、レビュー支援 |
| GitHub Issue | 変更理由、決定、進捗の記録 |
| Codex | リポジトリのルールに従う実装、自己検証、変更要約 |
| PR / Review | 変更内容・検証結果・残課題の確認と統合判断 |
| リポジトリ内ドキュメント | 現在の仕様・運用ルールの正本 |

## 標準フロー

### 1. 相談・要件整理（ChatGPT）

短い要望から始めてよい。ChatGPTは必要に応じて次を整理する。

- 解決したい問題と対象者
- 対象外・変更しないこと
- 既存機能への影響
- 実装方針と代替案
- 受入条件（完了と判断する具体的な条件）
- 人間の確認が必要な判断

仕様や判断が残す価値を持つ場合は、Issue本文または設計文書に記録する。

### 2. タスクを分類する（ChatGPT）

| 区分 | 例 | 進め方 |
| --- | --- | --- |
| 軽微 | 文言、表示、局所的な不具合 | Codexが実装・自己検証し、レビューで確認 |
| 通常 | 複数ファイルの機能追加・変更 | 要件・受入条件をIssueへ記録してから実装 |
| 重要 | 金額、権限、データ削除、外部連携、基幹業務ロジック | 実装前後に人間が方針・結果を確認 |

役割分担は必要なときだけ行う。調査、設計、実装、テスト、レビューを常に分離しない。

### 3. 履歴を用意する（GitHub Issue）

実装前に、可能な限りIssueを作る。Issueには次を残す。

- 背景・目的
- 実施内容と対象外
- 受入条件
- 重要な設計判断・注意点

思いつきで先に実装を始めた場合も、PRを作る前までに変更内容からIssueを後追い作成し、紐付ける。Issueの有無で作業を止めないが、履歴なしで完了しない。

### 4. 実装と自己検証（Codex）

CodexにはIssue、受入条件、該当する `AGENTS.md`、関連文書を渡す。Codexは次を行う。

1. 既存実装・関連文書・影響範囲を確認する。
2. 小さく実装し、要求外の変更を避ける。
3. 受入条件に照らし、build・test・lint・動作確認を可能な範囲で実施する。
4. 失敗を修正して再検証する（自己検証ループ）。
5. 未確認項目、リスク、判断待ちを隠さず報告する。

重要タスクでは、実装の前に方針、実装後に結果を人間が確認する。

### 5. PR / Review

PRには少なくとも次を記録する。

- Issueへのリンク
- 何を、なぜ変えたか
- 検証した内容と結果
- 未実施の確認・既知の制約
- 文書を更新したか、不要と判断した理由

Reviewでは、受入条件を満たすこと、不要な変更がないこと、重要なリスクが処理されていることを確認する。

### 6. 完了ゲートと文書更新

実装できただけでは完了としない。`TASK_COMPLETION_CHECKLIST.md` を使い、履歴・検証・文書更新を確認する。

次回も守るべきルール、現在の仕様、運用手順に変化があれば、該当する文書を更新する。

- リポジトリ固有の作業ルール: `AGENTS.md`
- 現在の仕様・設計: `docs/` などの設計文書
- 変更の背景・個別の判断: Issue / PR

一時的な調査メモや会話の全記録を恒久文書に写さない。再利用される事実・決定だけを残す。

## Codexへの依頼テンプレート

```text
Issue: #番号 または URL

目的:
- 何を解決したいか

受入条件:
- [ ] 条件1
- [ ] 条件2

制約・注意点:
- 変更してはいけないこと
- 人間確認が必要な判断

依頼:
関連する AGENTS.md と設計文書を確認し、影響範囲を調査してから実装してください。
実装後は受入条件に対して自己検証を行い、変更内容・検証結果・未確認事項・文書更新の要否を報告してください。
```

## 運用原則

- 長い完璧な指示より、目的・制約・受入条件を明確にする。
- 作業ルールはリポジトリ内のMarkdownを正本にする。
- AIの報告を完了の根拠にせず、検証結果とレビューで確認する。
- 失敗や手戻りから再利用できる学びだけを文書へ反映し、次回の品質を上げる。
